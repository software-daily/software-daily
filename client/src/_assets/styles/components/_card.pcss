/**
 * Card styles.
 */

$card-spacing: $section-spacing;

/* card list */

.card-list > li:not(:last-of-type) {
  margin-bottom: $card-spacing;
}

/* card item */

.card {
  background-color: rgba(0, 0, 0, .05);
  border: 1px solid $light-gray;
  border-radius: $atom;
  padding: $card-spacing;
  position: relative;
  transition: $transition-base;
  user-select: none;
}

.card:hover {
  background-color: $white;
}

.card > * {
  display: block;
}

/* title */

.card-title {
  color: $gray;
  margin-bottom: .25em;
}

.card-title:hover {
  color: $theme-4;
}

/* author */

.card-author {
  font-style: italic;
  margin-bottom: calc($atom * 6);
}

.card-author > a,
.card-comments {
  color: $gray;
  cursor: pointer;
  transition: $transition-base;
}

.card-author > a:hover,
.card-comments:hover {
  color: $link-hover-color;
}

.card-comments {
  float: left;
}

.card-comments > .fa {
  margin-right: calc($atom * 2);
}

.card-time {
  color: $light-gray;
  float: right;
}

/* active card (post detail is open) */

.is-active > .card {
  background-color: $theme-4;
  border-color: $white;
  color: $white;
}

.is-active .card-title {
  color: $white;
}

.is-active .card-author > a,
.is-active .card-comments,
.is-active .card-time {
  color: $lightest-gray;
}

.is-active .card-comments {
  cursor: default;
}
