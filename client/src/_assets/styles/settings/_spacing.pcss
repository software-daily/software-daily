/**
 * Spacing settings.
 */

/* units */
$atom: 2.5px;

/* elements */
$section-spacing: calc($atom * 10);
$dt-spacing: calc($atom * 4);
$header-height: 42px;
$input-height: 34px;

/* panels */
$controls-width: 400px;
$feed-width: 600px;
$details-width: 500px;
